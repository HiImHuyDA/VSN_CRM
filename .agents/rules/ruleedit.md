---
trigger: always_on
---

Không được phép viết query inline bên trong các module trong code backend và front end. Tất cả query đều phải xử lý qua store hoặc function trong sql server
tất cả chỉnh sửa phải đi theo test case, không được pass qua
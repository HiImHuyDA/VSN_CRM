# CSR Web Developer Guidelines & Project Rules

Welcome, Agent! This file contains essential architecture rules, environment configurations, and business logic requirements for the CSR Web (Customer Service Request) workspace. Please adhere to these strictly.

> [!IMPORTANT]
> **BẮT BUỘC KHI KHỞI ĐẦU PHIÊN LÀM VIỆC**:
> Mỗi lần bắt đầu một phiên làm việc hoặc nhận yêu cầu mới từ User, Agent **PHẢI ĐỌC KỸ** 2 file quy tắc dưới đây để thực hiện một cách chính xác tuyệt đối:
> 1. [AGENTS.md (Quy tắc Dự án & Công nghệ)](file:///d:/PM%20-%20PD%20Rev/project/csr-web/.agents/AGENTS.md)
> 2. [SESSION_HANDOFF_RULES.md (Quy tắc Bàn giao & Ghi nhận Context)](file:///d:/PM%20-%20PD%20Rev/project/csr-web/.agents/SESSION_HANDOFF_RULES.md)

---

## 🚀 1. Technology & Environments
- **Backend Port**: Running on port `3002` (configured in `backend/.env` under `PORT=3002`).
- **Frontend Port**: Vite dev/preview running on `4173`.
- **Proxy Configuration**: Full routing of `/api` requests to backend `http://localhost:3002` is configured in [vite.config.js](file:///D:/PM%20-%20PD%20Rev/project/csr-web/frontend/vite.config.js).
- **Process Management**: Processes are managed via PM2 (`ecosystem.config.js`). Use `pm2 restart all` to restart, or `pm2 delete all && pm2 start ecosystem.config.js` to clear environment caches.

---

## 💾 2. Database & Stored Procedure Rules
- **No Inline SQL**: Express routes in `backend/src/routes/` MUST NOT execute raw SQL queries inline. All operations must call Stored Procedures via `execute('usp_...')`.
- **Renamed & Standardized Procedures**: The old legacy SP names have been replaced with a standardized naming convention: `usp_[Module]_[Action]_[Scope]`. Use these names in code:
  - `usp_TaskConfig_GetDefaultsByDestinations` (loads task defaults for location list)
  - `usp_TaskConfig_List` (loads list of configs for system config admin UI)
  - `usp_Submission_GetDetail` (loads detailed view of a submission)
  - `usp_Submission_List` (loads paginated and filtered dashboard lists)
  - `usp_EmailTemplate_GetById` (gets single campaign email template)
  - `usp_EmailTemplate_List` (gets email campaign templates list)
  - `usp_TaskAttachment_GetById` (gets attachment by ID)
  - `usp_TaskAttachment_ListByTaskId` (lists task attachments)
  - `usp_Dashboard_GetStats` (generates dashboard charts & counter statistics)
- **Database Migrations**: All database schema changes should be saved in `backend/database/migrations/` and run using `node run_migration.js <migration_file.sql>`.

---

## 📊 3. SharePoint Excel Synchronization Workflow
- **Integration**: Sync is performed via the Microsoft Graph Excel Table API (`/workbook/tables/{table-name}/rows`).
- **Config Variables**: Defined in `backend/.env` under:
  - `SHAREPOINT_EXCEL_TABLE=Table1`
  - `SHAREPOINT_EXCEL_SHEET=Sheet1`
- **Matching Keys**: When a submission is edited, look up the existing row in SharePoint Excel matching:
  - `CSR_Id` + `ĐỊA ĐIỂM CÔNG TÁC` + `NGÀY ĐI`.
- **Preserve Vehicle Details**: If a matching row is found during edit, preserve any existing driver name, phone number, vehicle info, and start time columns. Do not overwrite them with blank data.
- **Attendee Calculation**:
  - `Total Attendees` = Count of staff in `Attendees` + `GuestCount` (number of guests).
- **Column Mappings**: Ensure correct index alignment when writing the 33 columns to the SharePoint Excel table row.

---

## ✉️ 4. Email Notifications
- **Trigger**: Sent automatically when BOD approves the hospitality request (`BOD đã duyệt`).
- **Template**: Formatted as a HTML table with mission details (Departure, Return, Destination, Attendees, Vehicle Info, Submitter).
- **Edit Workflow**: If a sheet is edited, check if schedule, destination, or date has changed. If so, resend the notification with the prefix `<div style="color: #d9381e;">Đơn tiếp khách có thay đổi như sau:</div>` above the details table.
- **Recipients**: Active assignees and supervisors of the task, except for Airport Shuttle at VSN-NT.

---

## 📂 5. Markdown File Placement Rule
- **Rule**: All developer documentation, guides, reports, walkthroughs, or system analyses in markdown (`.md`) format that are requested by the user or generated by the agent MUST be saved inside the `.agents/` folder.
- **Exception**: System standard git/npm files like `.gitignore` or `package.json` are unchanged.

---

## 📂 6. System Documentation
- **Tài liệu hệ thống**: [CSR_System_Documentation.md](file:///d:/PM%20-%20PD%20Rev/project/csr-web/.agents/CSR_System_Documentation.md) mô tả chi tiết kiến trúc ứng dụng (Frontend React + Backend Express + database SQL Server).
- **Quy tắc làm việc**: Mọi thay đổi nghiệp vụ, cấu hình hoặc sửa đổi database phải được cập nhật tương ứng vào tài liệu nghiệp vụ/kỹ thuật tương ứng nằm trong `.agents/`.

---

## 📦 7. Current Modules
Hệ thống hiện tại gồm các phân hệ chức năng:
1. **Quản Lý Tiếp Khách (Guest/Hospitality)**:
   - **Tạo mới & Sửa đơn**: [NewSubmission.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/NewSubmission.jsx), hỗ trợ phân loại Brand/Partner, đính kèm agenda và phân chia ngày di chuyển động.
   - **Quản lý lịch**: [GuestCalendar.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/GuestCalendar.jsx) hiển thị lịch tiếp đón theo tháng, phân loại trực quan theo màu sắc và loại khách.
   - **Quản lý công việc**: Quản lý các task chuẩn bị (Phòng họp, Khách sạn, Cơm trưa, Đưa đón) hỗ trợ Kanban và danh sách.
   - **Feedback Khảo Sát**: [FeedbackManagement.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/FeedbackManagement.jsx), tự động gửi link khảo sát bảo mật qua token, thu thập kết quả và đồng bộ điểm đánh giá.
2. **Cấu Hình Hệ Thống**:
   - **Danh mục**: Địa điểm, Khách hàng, Chiến dịch Email, Nhà hàng & ẩm thực, Phòng họp.
   - **Phân quyền Menu**: [MenuPermissionsConfig.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/config/MenuPermissionsConfig.jsx) quản lý ma trận quyền truy cập menu theo vai trò (BOD, PRD, User), tự động loang quyền cha-con, riêng vai trò `Admin` mặc định có toàn bộ quyền.
3. **Quản Lý Xe Công Tác (Fleet Management - Phase 1 MVP)**:
   - **Yêu cầu xe**: [VehicleBookingNew.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/vehicle/VehicleBookingNew.jsx), hỗ trợ đăng ký điểm đón, điểm đến, các điểm dừng trung gian động.
   - **Điều phối phê duyệt**: [VehicleBookingDetail.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/vehicle/VehicleBookingDetail.jsx) slide-over drawer hỗ trợ duyệt gán xe & tài xế từ danh mục sẵn sàng, tự động gửi email thông báo trạng thái.
   - **Báo cáo & Danh mục**: Xuất dữ liệu đặt xe ra Excel, cấu hình thông tin Xe, Tài xế, Loại xe tại [VehicleConfig.jsx](file:///d:/PM%20-%20PD%20Rev/project/csr-web/frontend/src/pages/vehicle/VehicleConfig.jsx).

---

## 🔮 8. Future Modules (Roadmap)
Các module dự kiến sẽ triển khai mở rộng cho phân hệ Quản Lý Xe:
- **Phase 2 — Smart Route**: Tích hợp OpenStreetMap + OSRM để tự động tính toán quãng đường (km), thời gian di chuyển dự kiến (ETA), và lưu trữ cache polyline lộ trình 30 ngày trong database.
- **Phase 3 — Smart Dispatch & Ride Pooling**:
  - Tự động gộp chuyến đi (Ride Pooling) nếu chênh lệch thời gian xuất phát <= 15 phút, khoảng cách điểm đón <= 1 km, độ tương đồng hướng tuyến >= 80% và xe còn chỗ trống.
  - Bộ máy đề xuất xe tối ưu (Assignment Engine) tính điểm tự động theo các trọng số (Khoảng cách, Mức ưu tiên, Ghế trống, Số giờ tài xế đã lái xe...).
- **Phase 4 — Realtime Tracking & Driver App**:
  - Giao diện driver ứng dụng web di động (PWA) gửi tọa độ GPS định kỳ 10 giây/lần.
  - Bản đồ giám sát trực tuyến (Live Map) sử dụng Leaflet/OpenStreetMap và Socket.IO cập nhật trực quan vị trí xe di chuyển trên bản đồ.
- **Phase 5 — Geofencing Automation**: Tự động nhận diện xe đi vào vùng đón khách (Zone Radius) và tự động thay đổi trạng thái booking (Đã đến → Đã đón → Hoàn thành) không cần tài xế thao tác tay.
- **Phase 6 — Analytics, Forecasting & OR-Tools**:
  - Dashboard thống kê hiệu suất sử dụng xe, tổng quãng đường, nhiên liệu tiết kiệm được nhờ gộp chuyến.
  - Dự đoán thời gian di chuyển (ETA Forecast) theo giờ cao điểm/lịch sử chuyến đi bằng thuật toán thống kê hoặc Scikit-learn.
  - Tối ưu hóa điều phối quy mô lớn bằng thư viện Google OR-Tools.

